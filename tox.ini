[tox]
envlist = py34, py35, py36

[testenv]
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/mmworkbench
deps =
    -r{toxinidir}/dev-requirements.txt
commands =
    pip install -U pip
    py.test --basetemp={envtmpdir}
    flake8 mmworkbench tests
